# Daily Memory — March 19, 2026 (James's time)

## Session Summary

### Timezone Correction
- James is in Orem, Utah — MDT (UTC-6), NOT GMT+8
- When server shows Friday March 20 ~4 AM, it's Thursday March 19 ~2 PM for James
- Updated USER.md, MEMORY.md, HEARTBEAT.md accordingly

### LeadHub — Full Audit + Polish Day

#### Riley (Frontend) completed:
- **SPA routing fix** — created `vercel.json` with rewrites so /signup, /dashboard no longer 404
- **Auth page UX overhaul** — prominent sign-up toggle, tagline, trust signals, polish
- **Header** — user email + sign out absorbed into header
- **Tabs** — horizontal scroll fix for mobile (8 tabs were cramped)
- **App.tsx** — cleaned up floating sign-out block

#### Alex (Backend) completed:
- **Bug fix:** `authService.ts` — `updateUserProfile` missing `.eq('id', user.id)` filter (would update all rows) — FIXED
- **Bug fix:** `vapi-webhook.ts` — `leads` insert missing `user_id` (NOT NULL constraint would fail) — FLAGGED + schema patch written
- **Schema:** Added missing RLS update policy on `calls` table, 6 performance indexes, schema patch section for `leads.user_id` nullable
- **Env var audit:** `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `VAPI_API_KEY`, `WEBHOOK_SECRET` still need to be added to Vercel

### Logo — Unresolved (parked)
- James's chosen logo: house/pin with cyan-blue left gradient, orange right gradient, white arrow, orange window panes, "HOME SERVICES LEAD GENERATION" tagline
- Multiple SVG attempts — none matched precisely
- Logged in `/workspace/memory/unresolved.md`
- Need original vector file OR more precise path tracing
- **Status:** Parked — revisit tomorrow

## Pending (James's Actions)
- [ ] Add Vercel env vars: SUPABASE_URL, SUPABASE_SERVICE_KEY, VAPI_API_KEY, WEBHOOK_SECRET
- [ ] Run schema patch SQL in Supabase (calls table + leads.user_id nullable + indexes)
- [ ] Create Vapi account → import assistant config → get phone number
- [ ] Provide original logo file if available (resolves unresolved.md item)
